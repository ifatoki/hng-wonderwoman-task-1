/* variables declaration

to make the returned string dynamic, it's useful to declare variables for parts of the string that can change, e.g. Name, HNG Id, language and stage number 

*/

var interName = "Motunrayo Ojo";
var internID = "HNG-00090";
var taskStage = 2;
var internLanguage = "Javascript";

console.log(`Hello World, this is ${interName} with HNGi7 ID ${internID} using ${internLanguage} for stage ${taskStage} task`);
const stageTwoTask = (fullName, id, language) => {
    if (fullName === 'Valerie Oakhu' && id === 'HNG-02011' && language === 'javascript') {
        return (`Hello World, this is ${fullName} with HNGi7 ID ${id} using ${language} for stage 2 task.`);
    }
}
console.log(stageTwoTask('Valerie Oakhu', 'HNG-02011', 'javascript'));
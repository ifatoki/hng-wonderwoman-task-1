const _name = `Maryam`;

const _email = ``;

const _id = ``;

const _language_ = `JavaScript`;

console.log(`Hello World, this is ${_name} with HNGi7 ID ${_id} and email ${_email} using ${_language_} for stage 2 task`);
